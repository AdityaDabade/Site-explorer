/**
 * Global sidebar is intentionally disabled for the traveler-facing redesign.
 * Admin now renders its own dashboard sidebar inside the page layout.
 */
export default function Sidebar() {
  return null;
}
